<style>
  .sidebar-container {
    display: flex;
    gap: 2rem;
    min-height: calc(100vh - 120px);
  }

  .sidebar {
    width: 280px;
    background-color: var(--white);
    border-radius: 16px;
    padding: 2rem 0;
    box-shadow: var(--shadow);
    position: sticky;
    top: 100px;
    height: fit-content;
    border-left: 5px solid var(--accent);
  }

  .sidebar-title {
    padding: 0 1.5rem;
    margin-bottom: 1.5rem;
    color: var(--primary-dark);
    font-size: 1.1rem;
    font-weight: 800;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .sidebar-title i {
    color: var(--accent);
    font-size: 1.3rem;
  }

  .sidebar-divider {
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--border), transparent);
    margin: 1rem 0;
  }

  .sidebar-nav {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .sidebar-item {
    position: relative;
    padding: 0 1rem;
  }

  .sidebar-link {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 0.9rem 1rem;
    color: var(--text);
    text-decoration: none;
    border-radius: 10px;
    transition: var(--transition);
    font-weight: 500;
    position: relative;
    overflow: hidden;
  }

  .sidebar-link::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0;
    width: 4px;
    height: 100%;
    background: var(--accent);
    transform: scaleY(0);
    transition: transform 0.3s ease;
    transform-origin: center;
  }

  .sidebar-link:hover::before {
    transform: scaleY(1);
  }

  .sidebar-link:hover {
    background-color: rgba(0, 122, 204, 0.1);
    color: var(--accent);
    padding-left: 1.5rem;
  }

  .sidebar-icon {
    font-size: 1.3rem;
    width: 30px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .sidebar-label {
    flex: 1;
    font-size: 0.9rem;
  }

  .sidebar-badge {
    font-size: 0.7rem;
    background-color: var(--accent);
    color: white;
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
    font-weight: 700;
  }

  .main-content-wrapper {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  @media (max-width: 1024px) {
    .sidebar {
      width: 240px;
      padding: 1.5rem 0;
    }

    .sidebar-link {
      padding: 0.8rem 0.75rem;
    }

    .sidebar-icon {
      font-size: 1.1rem;
    }

    .sidebar-label {
      font-size: 0.85rem;
    }
  }

  @media (max-width: 768px) {
    .sidebar-container {
      flex-direction: column;
      gap: 1rem;
    }

    .sidebar {
      width: 100%;
      border-radius: 12px;
      border-left: none;
      border-top: 5px solid var(--accent);
      padding: 1.5rem 0;
    }

    .sidebar-nav {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 0.5rem;
      padding: 0 1rem;
    }

    .sidebar-item {
      padding: 0;
    }

    .sidebar-link {
      flex-direction: column;
      text-align: center;
      gap: 0.5rem;
      padding: 1rem 0.75rem;
    }

    .sidebar-link::before {
      display: none;
    }

    .sidebar-link:hover::before {
      display: none;
    }

    .sidebar-icon {
      width: auto;
    }
  }

  @media (max-width: 576px) {
    .sidebar-nav {
      grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
    }

    .sidebar-label {
      font-size: 0.75rem;
    }

    .sidebar-icon {
      font-size: 1rem;
    }
  }
</style>

<div class="sidebar-container">
  <aside class="sidebar">
    <div class="sidebar-title">
      <i class="fas fa-bolt"></i> Quick Actions
    </div>
    
    <div class="sidebar-divider"></div>
    
    <nav class="sidebar-nav">
      <div class="sidebar-item">
        <a href="/addPackage" class="sidebar-link" title="Create a new package">
          <i class="sidebar-icon fas fa-box-open"></i>
          <span class="sidebar-label">Add Package</span>
        </a>
      </div>

      <div class="sidebar-item">
        <a href="/addProcess" class="sidebar-link" title="Setup a new process">
          <i class="sidebar-icon fas fa-cogs"></i>
          <span class="sidebar-label">Add Process</span>
        </a>
      </div>

      <div class="sidebar-item">
        <a href="/add-department" class="sidebar-link" title="Create a new department">
          <i class="sidebar-icon fas fa-sitemap"></i>
          <span class="sidebar-label">Add Department</span>
        </a>
      </div>

      <div class="sidebar-item">
        <a href="/addUser" class="sidebar-link" title="Invite a new user">
          <i class="sidebar-icon fas fa-user-plus"></i>
          <span class="sidebar-label">Add User</span>
        </a>
      </div>

      <div class="sidebar-item">
        <a href="/check-users" class="sidebar-link" title="View all users">
          <i class="sidebar-icon fas fa-users"></i>
          <span class="sidebar-label">View Users</span>
        </a>
      </div>

      <div class="sidebar-item">
        <a href="/workFlowDash" class="sidebar-link" title="Create a new workflow">
          <i class="sidebar-icon fas fa-tasks"></i>
          <span class="sidebar-label">Add Workflow</span>
        </a>
      </div>

      <div class="sidebar-item">
        <a href="/addProject" class="sidebar-link" title="Start a new project">
          <i class="sidebar-icon fas fa-project-diagram"></i>
          <span class="sidebar-label">Add Project</span>
        </a>
      </div>
    </nav>
  </aside>

  <div class="main-content-wrapper">
